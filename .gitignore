node_modules
dist
.next
.turbo

# secrets — never commit any .env file with real values
.env
.env.local
.env.production
.env.production.local
.env.development.local
.env.test.local
.env*
!.env.example

*.log
coverage
.DS_Store

# Stripe CLI event logs
.stripe/

# Runtime app data (auth users, etc.) — never commit; may contain PII / hashes
.data/
**/.data/
